<Page x:Class="SupermarketPOS.UI.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      Background="{DynamicResource BackgroundBrush}"
      Unloaded="Page_Unloaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- 0: Header -->
                <RowDefinition Height="Auto"/> <!-- 1: Quick Access -->
                <RowDefinition Height="Auto"/> <!-- 2: KPI 1 -->
                <RowDefinition Height="Auto"/> <!-- 3: KPI 2 -->
                <RowDefinition Height="Auto"/> <!-- 4: Charts -->
                <RowDefinition Height="Auto"/> <!-- 5: Invoices -->
                <RowDefinition Height="Auto"/> <!-- 6: Loading -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource PrimaryBrush}" CornerRadius="16" Padding="24,20" Margin="0,0,0,24">
                <Grid>
                    <StackPanel>
                        <TextBlock x:Name="GreetingText" FontSize="28" FontWeight="Bold" Foreground="White"/>
                        <TextBlock x:Name="DateText" FontSize="14" Foreground="#B0B0B0" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button x:Name="BtnRefresh" Content="🔄 تحديث" Style="{StaticResource SecondaryButtonStyle}" Foreground="White" Width="120" Height="40" Click="RefreshButton_Click" Margin="0,0,12,0"/>
                        <Button x:Name="BtnDateRange" Content="📅 اليوم" Style="{StaticResource SecondaryButtonStyle}" Foreground="White" Width="130" Height="40" Click="DateRange_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Quick Access Row -->
            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🚀 وصول سريع:" Foreground="{DynamicResource SidebarSubTextBrush}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <WrapPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="👥 الموظفين" Style="{StaticResource GhostButtonStyle}" Margin="0,0,8,0" Click="QuickAccess_Employees_Click"/>
                    <Button Content="⏱️ الحضور" Style="{StaticResource GhostButtonStyle}" Margin="0,0,8,0" Click="QuickAccess_Attendance_Click"/>
                    <Button Content="💰 الرواتب" Style="{StaticResource GhostButtonStyle}" Margin="0,0,8,0" Click="QuickAccess_Payroll_Click"/>
                    <Button Content="🎯 العملاء المحتملين" Style="{StaticResource GhostButtonStyle}" Margin="0,0,8,0" Click="QuickAccess_Leads_Click"/>
                    <Button Content="📈 مراحل البيع" Style="{StaticResource GhostButtonStyle}" Margin="0,0,8,0" Click="QuickAccess_Pipeline_Click"/>
                </WrapPanel>
            </Grid>

            <!-- KPI Cards Row 1 -->
            <Grid Grid.Row="2" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource KpiCardStyle}">
                    <StackPanel Margin="20">
                        <TextBlock Text="💰 المبيعات" Style="{StaticResource KpiLabelStyle}"/>
                        <TextBlock x:Name="KpiSales" Style="{StaticResource KpiValueStyle}"/>
                        <TextBlock x:Name="KpiSalesTrend" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource KpiCardStyle}">
                    <StackPanel Margin="20">
                        <TextBlock Text="📈 الأرباح" Style="{StaticResource KpiLabelStyle}"/>
                        <TextBlock x:Name="KpiProfit" Style="{StaticResource KpiValueStyle}"/>
                        <TextBlock x:Name="KpiProfitTrend" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="4" Style="{StaticResource KpiCardStyle}">
                    <StackPanel Margin="20">
                        <TextBlock Text="📦 المخزون" Style="{StaticResource KpiLabelStyle}"/>
                        <TextBlock x:Name="KpiInventory" Style="{StaticResource KpiValueStyle}"/>
                        <TextBlock x:Name="KpiInventoryAlert" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="6" Style="{StaticResource KpiCardStyle}">
                    <StackPanel Margin="20">
                        <TextBlock Text="🧾 الفواتير" Style="{StaticResource KpiLabelStyle}"/>
                        <TextBlock x:Name="KpiInvoices" Style="{StaticResource KpiValueStyle}"/>
                        <TextBlock x:Name="KpiInvoicesTrend" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- KPI Cards Row 2 -->
            <Grid Grid.Row="3" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource KpiCardStyle}">
                    <StackPanel Margin="20">
                        <TextBlock Text="🛒 المشتريات" Style="{StaticResource KpiLabelStyle}"/>
                        <TextBlock x:Name="KpiPurchases" Style="{StaticResource KpiValueStyle}"/>
                        <TextBlock Text="إجمالي المشتريات" FontSize="12" Foreground="{DynamicResource SidebarSubTextBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource KpiCardStyle}">
                    <StackPanel Margin="20">
                        <TextBlock Text="👥 الموارد البشرية" Style="{StaticResource KpiLabelStyle}"/>
                        <TextBlock x:Name="KpiEmployees" Style="{StaticResource KpiValueStyle}"/>
                        <TextBlock Text="إجمالي الموظفين" FontSize="12" Foreground="{DynamicResource SidebarSubTextBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="4" Style="{StaticResource KpiCardStyle}">
                    <StackPanel Margin="20">
                        <TextBlock Text="🎯 علاقات العملاء" Style="{StaticResource KpiLabelStyle}"/>
                        <TextBlock x:Name="KpiLeads" Style="{StaticResource KpiValueStyle}"/>
                        <TextBlock Text="العملاء المحتملين" FontSize="12" Foreground="{DynamicResource SidebarSubTextBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="6" Background="Transparent" BorderThickness="0"/>
            </Grid>

            <!-- Charts Row — NO XAML BINDINGS (set in code-behind) -->
            <Grid Grid.Row="4" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource ChartCardStyle}" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="📊 اتجاه المبيعات" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,12"/>
                        <lvc:CartesianChart x:Name="SalesChart" Grid.Row="1" Height="320"/>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource ChartCardStyle}" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="🥧 توزيع المبيعات" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,12"/>
                        <lvc:PieChart x:Name="CategoryChart" Grid.Row="1" Height="320"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Recent Invoices -->
            <Border Grid.Row="5" Style="{StaticResource ChartCardStyle}" Margin="0,0,0,24" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="🧾 آخر الفواتير" Style="{StaticResource CardHeaderStyle}"/>
                        <Button Content="عرض الكل →" Style="{StaticResource GhostButtonStyle}" HorizontalAlignment="Right" Click="ViewAllInvoices_Click"/>
                    </Grid>
                    <DataGrid Grid.Row="1" x:Name="RecentInvoicesGrid" AutoGenerateColumns="False" IsReadOnly="True" RowHeight="44"
                              Background="Transparent" HeadersVisibility="Column" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceNumber}" Width="140"/>
                            <DataGridTextColumn Header="العميل" Binding="{Binding CustomerName}" Width="200"/>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat=dd/MM/yyyy HH:mm}" Width="160"/>
                            <DataGridTextColumn Header="الإجمالي" Binding="{Binding Total, StringFormat=N2}" Width="130">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" Grid.Row="6" Background="#99000000" Visibility="Collapsed" Panel.ZIndex="100">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="جاري التحميل..." FontSize="16" Foreground="White" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>