<local:BaseWindow x:Class="SupermarketPOS.UI.StockTakeWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:local="clr-namespace:SupermarketPOS.UI"
                  Title="جرد المخزون"
                  WindowState="Maximized"
                  WindowStyle="None"
                  FlowDirection="RightToLeft"
                  Background="{DynamicResource BackgroundBrush}">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <Grid x:Name="MainGrid" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource HeaderBorderStyle}" Margin="0,0,0,16">
            <Grid>
                <TextBlock Text="📝 جرد المخزون" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <Button Content="✕" Width="40" Height="40" Style="{StaticResource IconButtonStyle}" Foreground="White" HorizontalAlignment="Right" Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
            <WrapPanel>
                <TextBlock Text="المخزن:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="WarehouseCombo" Width="200" Margin="0,0,16,0"/>
                <TextBlock Text="بحث:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="SearchBox" Width="250" Margin="0,0,16,0"/>
                <Button Content="🔄 تحميل" Style="{StaticResource SecondaryButtonStyle}" Width="100" Click="LoadButton_Click"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
            <WrapPanel HorizontalAlignment="Center">
                <TextBlock x:Name="TotalItemsText" Margin="0,0,30,0"/>
                <TextBlock x:Name="TotalQuantityText" Margin="0,0,30,0"/>
                <TextBlock x:Name="TotalValueText"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16" Padding="0">
            <DataGrid x:Name="StockTakeGrid" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الكود" Binding="{Binding ProductId}" Width="60"/>
                    <DataGridTextColumn Header="الباركود" Binding="{Binding Barcode}" Width="120"/>
                    <DataGridTextColumn Header="اسم المنتج" Binding="{Binding ProductName}" Width="200"/>
                    <DataGridTextColumn Header="الرصيد الحالي" Binding="{Binding SystemQuantity}" Width="100"/>
                    <DataGridTextColumn Header="الكمية الفعلية" Binding="{Binding ActualQuantity}" Width="100"/>
                    <DataGridTextColumn Header="الفرق" Binding="{Binding Difference}" Width="80"/>
                    <DataGridTextColumn Header="قيمة الفرق" Binding="{Binding DifferenceValue, StringFormat={}{0:N2}}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource CardBorderStyle}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="❌ إلغاء" Style="{StaticResource SecondaryButtonStyle}" Width="110" Click="CloseButton_Click"/>
                <Button Content="💾 حفظ الجرد" Style="{StaticResource SuccessButtonStyle}" Width="130" Margin="8,0,0,0" Click="SaveButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</local:BaseWindow>