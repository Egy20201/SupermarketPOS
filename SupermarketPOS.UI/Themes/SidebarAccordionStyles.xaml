<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ==================== SIDEBAR COLORS ==================== -->
    <SolidColorBrush x:Key="SidebarBgBrush" Color="#0F141E"/>
    <SolidColorBrush x:Key="SidebarHeaderTextBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SidebarItemTextBrush" Color="#B0B8C5"/>
    <SolidColorBrush x:Key="SidebarHoverBgBrush" Color="#1A2332"/>
    <SolidColorBrush x:Key="SidebarActiveBgBrush" Color="#1A3A2A"/>
    <SolidColorBrush x:Key="SidebarActiveIndicatorBrush" Color="#10B981"/>
    <SolidColorBrush x:Key="SidebarDividerBrush" Color="#2A3442"/>
    <SolidColorBrush x:Key="SidebarIconBrush" Color="#6B7A8F"/>
    <SolidColorBrush x:Key="SidebarArrowBrush" Color="#6B7A8F"/>

    <!-- ==================== SIDEBAR EXPANDER STYLE (ACCORDION HEADER) ==================== -->
    <Style x:Key="SidebarExpanderStyle" TargetType="Expander">
        <Setter Property="Foreground" Value="{StaticResource SidebarHeaderTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0,2,0,2"/>
        <Setter Property="IsExpanded" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header ToggleButton -->
                        <ToggleButton x:Name="ExpanderButton" 
                                      Grid.Row="0"
                                      IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      HorizontalContentAlignment="Stretch"
                                      Padding="12,12,16,12"
                                      Cursor="Hand"
                                      Foreground="{TemplateBinding Foreground}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="border" 
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="8"
                                            Margin="8,2,8,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Icon -->
                                            <ContentPresenter x:Name="iconPresenter" 
                                                              Grid.Column="0"
                                                              Content="{Binding Tag, RelativeSource={RelativeSource AncestorType=Expander}}"
                                                              Margin="0,0,12,0"
                                                              VerticalAlignment="Center"/>

                                            <!-- Header Text -->
                                            <ContentPresenter Grid.Column="1" 
                                                              Content="{Binding Header, RelativeSource={RelativeSource AncestorType=Expander}}"
                                                              HorizontalAlignment="Left" 
                                                              VerticalAlignment="Center"
                                                              TextBlock.FontSize="13"
                                                              TextBlock.FontWeight="Bold"
                                                              TextBlock.Foreground="{StaticResource SidebarHeaderTextBrush}"/>

                                            <!-- Arrow -->
                                            <Border x:Name="arrowContainer" 
                                                    Grid.Column="2"
                                                    Width="20" 
                                                    Height="20"
                                                    CornerRadius="4">
                                                <TextBlock x:Name="Arrow" 
                                                           Text="▶" 
                                                           FontSize="10"
                                                           Foreground="{StaticResource SidebarArrowBrush}"
                                                           HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <!-- Expanded State - Arrow Down -->
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Arrow" Property="Text" Value="▼"/>
                                        </Trigger>
                                        <!-- Hover -->
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="{StaticResource SidebarHoverBgBrush}"/>
                                            <Setter TargetName="Arrow" Property="Foreground" Value="#FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <!-- Content with Animation -->
                        <ContentPresenter x:Name="ExpanderContent" 
                                          Grid.Row="1"
                                          Visibility="Collapsed"
                                          Margin="8,0,8,8">
                            <ContentPresenter.Resources>
                                <Storyboard x:Key="ExpandAnimation">
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.2"/>
                                </Storyboard>
                                <Storyboard x:Key="CollapseAnimation">
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="1" To="0" Duration="0:0:0.15"/>
                                </Storyboard>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ExpanderContent" 
                                                                       Storyboard.TargetProperty="Visibility">
                                            <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                        <DoubleAnimation Storyboard.TargetName="ExpanderContent" 
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ExpanderContent" 
                                                         Storyboard.TargetProperty="Opacity"
                                                         From="1" To="0" Duration="0:0:0.15"/>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ExpanderContent" 
                                                                       Storyboard.TargetProperty="Visibility">
                                            <DiscreteObjectKeyFrame KeyTime="0:0:0.15" Value="{x:Static Visibility.Collapsed}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== SIDEBAR MENU ITEM STYLE ==================== -->
    <Style x:Key="SidebarMenuItemStyle" TargetType="Button">
        <Setter Property="Height" Value="38"/>
        <Setter Property="Margin" Value="0,1,0,1"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource SidebarItemTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="16,0,0,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Active Indicator -->
                            <Border x:Name="activeIndicator" 
                                    Grid.Column="0" 
                                    Background="Transparent" 
                                    CornerRadius="2"
                                    Width="3"
                                    HorizontalAlignment="Left"/>

                            <!-- Content -->
                            <ContentPresenter Grid.Column="1" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource SidebarHoverBgBrush}"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                        <Trigger Property="Tag" Value="Active">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource SidebarActiveBgBrush}"/>
                            <Setter TargetName="activeIndicator" Property="Background" Value="{StaticResource SidebarActiveIndicatorBrush}"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== SIDEBAR DIVIDER ==================== -->
    <Style x:Key="SidebarDividerStyle" TargetType="Border">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Background" Value="{StaticResource SidebarDividerBrush}"/>
        <Setter Property="Margin" Value="16,8,16,8"/>
    </Style>

</ResourceDictionary>

